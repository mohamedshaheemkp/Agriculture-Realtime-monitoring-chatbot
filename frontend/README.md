# Frontend - Agriculture Monitoring System

This directory contains the React.js frontend for the **Smart Agriculture Monitoring & AI Chatbot** system.

## 🚀 Quick Start

1.  **Install Dependencies**:
    ```bash
    npm install
    ```

2.  **Start the Development Server**:
    ```bash
    npm start
    ```
    The application will run at [http://localhost:3000](http://localhost:3000).

## 📂 Project Structure

*   `src/components/`: Contains UI components like `Dashboard.js`, `Chatbot.js`, and `VideoFeed.js`.
*   `src/App.js`: Main application entry point.

For full system documentation, usage guide, and architecture, please refer to the [Root README](../README.md).


`How to run the program`
1st backend :
    cd backend
    source .venv/bin/activate
    pip install -r requirements.txt
    python app.py
2nd backend :
    cd frontend
    npm install
    npm start
    
